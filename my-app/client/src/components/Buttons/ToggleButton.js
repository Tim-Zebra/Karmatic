import React from 'react'
import { StyledButton } from './Button.styled'

export default function ToggleButton() {
    return(
        <StyledButton>
          <button
          type="submit"
          > TOGGLE ME

          </button>
        </StyledButton>
    )
}